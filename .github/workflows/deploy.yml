name: Deploy to Production

on:
    push:
        branches:
            - prod

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: Install cloudflared
              run: |
                  curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o cloudflared.deb
                  sudo dpkg -i cloudflared.deb

            - name: Setup SSH key
              run: |
                  mkdir -p ~/.ssh
                  echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
                  chmod 600 ~/.ssh/id_ed25519
                  cat >> ~/.ssh/config <<EOF
                  Host ssh.codingclub.in
                      ProxyCommand cloudflared access ssh --hostname %h
                      StrictHostKeyChecking no
                      UserKnownHostsFile /dev/null
                  EOF

            - name: Deploy
              run: |
                  ssh cc@ssh.codingclub.in << 'EOF'
                    cd ~/Projects/CourseHub
                    git pull

                    node server/scripts/preflight.js || exit 1
                    npm ci || exit 1

                    # Frontend
                    npm run build:student || exit 1
                    rm -rf /var/www/coursehub/client/*
                    cp -r client/dist/* /var/www/coursehub/client/

                    # Admin
                    npm run build:admin || exit 1
                    rm -rf /var/www/coursehub/admin/*
                    cp -r admin/dist/* /var/www/coursehub/admin/

                    pm2 reload coursehub-backend
                  EOF
